apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: airbnb-app
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: lab2-airbnb-backend:latest
        imagePullPolicy: Never  # Use local image from docker-compose build
        ports:
        - containerPort: 4000
          name: http
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: airbnb-config
              key: NODE_ENV
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: airbnb-config
              key: PORT
        - name: MYSQL_HOST
          valueFrom:
            configMapKeyRef:
              name: airbnb-config
              key: MYSQL_HOST
        - name: MYSQL_PORT
          valueFrom:
            configMapKeyRef:
              name: airbnb-config
              key: MYSQL_PORT
        - name: MYSQL_DB
          valueFrom:
            configMapKeyRef:
              name: airbnb-config
              key: MYSQL_DB
        - name: AI_AGENT_URL
          valueFrom:
            configMapKeyRef:
              name: airbnb-config
              key: AI_AGENT_URL
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: airbnb-secrets
              key: MYSQL_USER
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: airbnb-secrets
              key: MYSQL_PASSWORD
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: airbnb-secrets
              key: SESSION_SECRET
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 20
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: airbnb-app
  labels:
    app: backend
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
